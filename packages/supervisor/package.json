{
  "name": "@crash-proof/supervisor",
  "version": "1.0.0",
  "main": "./dist/handler.js",
  "types": "./dist/handler.d.ts",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js --external:pg-native",
    "build:types": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@crash-proof/shared": "*",
    "@google/generative-ai": "^0.21.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}
